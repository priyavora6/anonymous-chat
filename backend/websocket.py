"""Simple websocket utilities and Socket.IO placeholder.
This file is a scaffold kept for compatibility with the requested repo layout.
Current FastAPI websocket implementation lives in `app/main.py`.
"""
from typing import Any

def info():
    return {"note": "Primary websocket handlers are in app/main.py (FastAPI)."}
